{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "To-do list app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm --prefix ./server install ./server/package/json && npm --prefix ./client install ./client/package/json",
    "build": "npm run build --prefix client/",
    "deploy": "npm start --prefix client/",
    "database": "docker compose -f ./server/src/docker-compose.yaml -p database up -d"
  },
  "author": "Yakir Hizkiyahu",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
