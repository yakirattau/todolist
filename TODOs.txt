1. API gateway (node-http-proxy)
2. Load balancer (NodeJS and nginx)
3. Node and app container image for docker

nginx

apt update && apt install -y vim && mkdir /usr/share/site/ && touch /usr/share/site/index.html && echo "<html><body><h1>Hello</h1></body></html>" > /usr/share/site/index.html && cp /etc/nginx/nginx.conf /root/ && cp /etc/nginx/conf.d/default.conf /root/


create index.html
	